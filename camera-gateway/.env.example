# Configuración local de la pasarela de cámara en `pihomeblk-1`.
# Copiar a `.env`. El fichero real no entra en Git.

# IP Tailscale de la Raspberry. El puerto 1984 se publica solo en esta IP:
# nunca 0.0.0.0, que lo dejaría accesible desde la LAN doméstica.
GATEWAY_BIND_IP=100.x.y.z
